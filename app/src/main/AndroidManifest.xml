<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.products.ammar.sheikhsha3ban">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.SignUpActivity"
            android:label="Sign up" />

        <activity
            android:name=".auth.LoginActivity"
            android:label="@string/login" />
        <activity
            android:name=".profile.ProfileActivity"
            android:label="@string/profile" />
        <activity android:name=".attendance.viewer.ViewerAttendanceActivity" />
        <activity android:name=".attendance.admin.AdminAttendanceActivity" android:exported="true" />
        <activity android:name=".attendance.AttendanceActivity" />
    </application>

</manifest>